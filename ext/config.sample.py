# mysql auth info
mysql = {
    'db': 'asahi',
    'host': 'localhost',
    'password': 'ape',
    'user': 'tsunyoku'
}

# port for web server to run on (used for nginx connections)
# you can really set this to anything that isn't used, it really does not matter as long as you reflect it in your nginx config
port = 9384

# debug is used to print more info & help to debug if you are experiencing issues with the server
debug = True